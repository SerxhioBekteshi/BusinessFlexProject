import { Typography } from "@mui/material";
import { styled } from "@mui/material/styles";

export const TypographyErrorStyled = styled(Typography)(({ theme }) => ({
  color: "red",
  fontSize: "0.8rem",
}));
